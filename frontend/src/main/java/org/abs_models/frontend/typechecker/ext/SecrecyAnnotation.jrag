import org.abs_models.frontend.typechecker.ext.SecrecyExtension;

/**
 * Our way to handover the user input for the --secrecy option to the SecrecyAnnotationChecker
 * null if there is no user input
 *
 * @author Maximilian Paul
 */
aspect SecrecyAnnotation {
    public SecrecyExtension Model.secrecyExtension = null;
}